backend/app/graph
本目录负责：生成流程状态机与节点编排。
不负责：HTTP 入口、模板渲染实现。声明：一旦本目录结构或职责发生变化，请更新本文件。

文件说明
`__init__.py` 地位：graph 包出口。职责：导出状态图构建接口。
`generation_graph.py` 地位：流程中枢。职责：定义生成流程节点、分支和任务推进。
`generation_state.py` 地位：流程状态定义。职责：约束图节点共享的数据结构。
