frontend/src/pages
本目录负责：页面级视图与交互。
不负责：底层 API 实现与全局应用装配。声明：一旦本目录结构或职责发生变化，请更新本文件。

文件说明
`CreateProject.jsx` 地位：首页创建页。职责：输入文章并创建项目。
`EditScene.jsx` 地位：分镜编辑页。职责：编辑单个场景内容并触发保存。
`GenerationProgress.jsx` 地位：生成进度页。职责：展示任务阶段与轮询结果。
`Result.jsx` 地位：结果页。职责：展示生成结果和视频地址。
